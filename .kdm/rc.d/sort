# Functions: Sort

[[ "${_}" != "${0}" && "${SOURCE_VERBOSE}" == "true" ]] && output kv "Loaded" "${BASH_SOURCE[@]/$HOME\/\.kdm\//}"


# Sort file in-place
_sort_in_place() {
	sort -o "${1}" "${1}"
}


alias sort-in-place='_sort_in_place'


# vim: set filetype=sh ts=2 sw=2 tw=0 noet :
